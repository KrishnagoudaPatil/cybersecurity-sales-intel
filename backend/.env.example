# Copy to backend/.env and fill in. Without a key the app runs in deterministic MOCK
# mode (evals, tracing, cost all still work). With a key it calls the real models.
ANTHROPIC_API_KEY=
MODEL_CLASSIFY=claude-haiku-4-5-20251001
MODEL_JUDGE=claude-sonnet-5
